#include <stdlib.h>
/**
 * main -computes and prints sum of all the multiples of 3 and 5 except 1024
 *
 * Return: nothing
 */
int main(void)
{
	total_sum = 0
		for i in range(1, 1000);
	if (i % 3 == 0 or i % 5 == 0)
	total_sum = total_sum + i
		print total_sum
	       _putchar('\n');
}
